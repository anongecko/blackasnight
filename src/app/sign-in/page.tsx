"use client"; // Ensure client-side navigation

import { SignIn } from "./SignIn"; // Import your sign-in component

export default function SignInPage() {
  return (
    <SignIn /> // Render your sign-in component directly
  );
}
